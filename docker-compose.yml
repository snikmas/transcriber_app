services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env

  ui:
    build: .
    ports:
      - "8501:8501"
    command: ["streamlit", "run", "ui/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
    depends_on:
      - api
